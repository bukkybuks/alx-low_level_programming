#include "main.h"
/**
 * add - adds two numbers together
 * @num1: first number
 * @num2: second number
 * Return: returns the result of the addition
 */
int add(int num1, int num2)
{
	int total;

	total = num1 + num2;
	return (total);
}
