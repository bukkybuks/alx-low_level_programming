#include "main.h"
/**
 * _islower - checks if a character is in lowercase
 * @c: character to be checked
 * Return: 1 if character is in lowercase, and 0 otherwise
 */
int _islower(int c)
{
	if (_islower(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
